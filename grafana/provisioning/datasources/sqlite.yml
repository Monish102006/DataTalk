apiVersion: 1

datasources:
  - name: DataTalk SQLite
    type: frser-sqlite-datasource
    access: proxy
    url: /var/lib/backend-data/datatalk.db
    isDefault: true
    jsonData:
      path: /var/lib/backend-data/datatalk.db
